import {gallery} from "../assets/assets"


const ServicePage = () => {
    return (
        <section className='bg-zinc-800 px-8 md:px-12 py-2                                                  '>
            <div className="container flex flex-col md:flex-cols-5 gap-3 items-center justify-center">
                [
gallery.slice(0).map((item, index) => (
    .
))            
                ]
            </div>
        </section>

    )
}

export default ServicePage